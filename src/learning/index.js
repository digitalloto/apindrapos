/**
 * UPIE — Learning System
 * Patent Pending — AIMCRS
 */

const FlightRecorder = require('./flight-recorder');
const LearningEngine = require('./learning-engine');

module.exports = { FlightRecorder, LearningEngine };
